'use strict';

const walkingCodeMovie = require('..');
const assert = require('assert').strict;

assert.strictEqual(walkingCodeMovie(), 'Hello from walkingCodeMovie');
console.info('walkingCodeMovie tests passed');
